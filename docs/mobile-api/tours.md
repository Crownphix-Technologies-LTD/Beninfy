# Tours

Implemented endpoints:

- `GET /api/mobile/v1/tours`
- `GET /api/mobile/v1/tours/:tourId`

These expose the existing public tour catalogue to Flutter.

Tour booking and tour payment are not implemented in this phase because the current backend has catalogue content but no authoritative tour booking, inventory, itinerary, or payment lifecycle model.
